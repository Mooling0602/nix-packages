# reasonix-go

Go 重构版 [reasonix](https://github.com/esengine/deepseek-reasonix)（`main-v2` 分支），本 Nix 包名为 `reasonix-go` 以与 npm 版本区分，上游尚未发布 Go 版本的正式 release，当前使用主分支最新 commit 构建。

源项目（`main-v2` 分支的 Go 重构版）仍处于快速迭代中，待上游打出稳定 tag 后可更新 `rev` 和 `version`。
